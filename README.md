# 🎮 NEON MuOnline - Admin Panel & Website

Современная админ-панель и сайт для приватного сервера MuOnline с поддержкой мультиязычности.


Быстрый старт

### 1. Установка зависимостей:
```bash
npm install
cd frontend
npm install
cd ..
```

### 2. Настройка базы данных:
```bash
node database/run-migrations.js
node database/run-seeds.js
```

### 3. Запуск серверов:

**Терминал 1 - Backend (порт 3000):**
```bash
node backend/src/server.js
```

**Терминал 2 - Frontend (порт 5173):**
```bash
cd frontend
npm run dev
```

### 4. Открыть в браузере:
```
http://localhost:5173/admin/editing/index.html
```

**Тестовый аккаунт:**
- Email: `admin@neon.mu`
- Пароль: `admin123`

---

## Что работает

### Админ-панель - Страница редактирования (`/admin/editing`)
**Полностью функциональна! 100% готова к работе.**

- Создание и редактирование страниц
- Создание и редактирование блоков контента
- Добавление объектов (текст, изображения, списки)
- Drag & Drop для изменения порядка объектов
- Мультиязычность (RU/EN)
- Создание переводов для страниц и блоков
- Загрузка изображений (с компьютера или по URL)
- Адаптивная верстка (работает на мобильных)
- Авторизация через JWT
- огирование всех действий

### Другие страницы:
- `/admin/main` - просмотр страниц (верстка требует доработки)
- `/for_users` - публичная страница (верстка требует доработки)
- `/login` - страница входа (работает)

---

## Известные проблемы

1. **Мобильная верстка** - сломана на `/admin/main` и `/for_users`
   - Причина: забыл про адаптив при рефакторинг

2. **Стили** - некоторые элементы отображаются не идеально
   - Причина: сегодня фиксил баги и немного сломал стили
   - Исправление: вместе с мобильной версткой

3. **Не реализовано:**
   - Управление предметами (Items)
   - Управление категориями
   - Поиск по сайту
   - Удаление страниц через UI


## 🛠️ Полезные команды

### Сброс базы данных:
```bash
del database\neon_muonline.db
node database/run-migrations.js
node database/run-seeds.js
```

### Просмотр логов:
```bash
tail -f backend/logs/app.log
```

### Проверка БД:
```bash
sqlite3 database/neon_muonline.db
SELECT * FROM pages;
.quit
```

### Тестирование API:
```bash
# Получить список страниц
curl http://localhost:3000/api/pages?lang=ru

# Авторизация
curl -X POST http://localhost:3000/api/admin/auth/login ^
  -H "Content-Type: application/json" ^
  -d "{\"email\":\"admin@neon.mu\",\"password\":\"admin123\"}"
```

---

## API Endpoints

### Public API:
- `GET /api/pages` - список страниц
- `GET /api/pages/:slug` - одна страница
- `GET /api/pages/:slug/blocks` - блоки страницы
- `GET /api/items` - список предметов

### Admin API (требует авторизации):
- `POST /api/admin/auth/login` - вход
- `GET /api/admin/auth/me` - текущий пользователь
- `GET /api/admin/pages` - список страниц
- `POST /api/admin/pages` - создать страницу
- `PUT /api/admin/pages/:id` - обновить страницу
- `GET /api/admin/blocks` - список блоков
- `POST /api/admin/blocks` - создать блок
- `PUT /api/admin/blocks/:id` - обновить блок
- `DELETE /api/admin/blocks/:id` - удалить блок
- `POST /api/admin/upload/image` - загрузить изображение



### Проблема: Сервер не запускается
```bash
# Проверить порты
netstat -ano | findstr :3000
netstat -ano | findstr :5173

# Убить процесс если занят
taskkill /PID <PID> /F
```

### Проблема: БД не создается
```bash
# Проверить права доступа к папке database/
# Удалить и пересоздать
del database\neon_muonline.db
node database/run-migrations.js
```

### Проблема: 401 Unauthorized
```bash
# Проверить токен в localStorage
# Перелогиниться
# Проверить что backend запущен
```

---

Следующие шаги

Исправить мобильную верстку на всех страницах
Поправить стили на `/admin/main` и `/for_users`
Добавить кнопку удаления страниц

Реализовать управление предметами (Items)
Добавить управление категориями
Оптимизировать производи